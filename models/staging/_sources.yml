version: 2
sources:
  - name: raw
    # Choose DB by active target (dev/stg/prod)
    database: "{{ target.name | upper }}_DB"
    schema: RAW
    tables:
      - name: orders
      - name: customers
